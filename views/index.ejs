<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/3d8af50c2d.js" crossorigin="anonymous"></script>

    <link rel="stylesheet" href="css/bs/bootstrap.min.css">
    <link rel="stylesheet" href="css/bs/bootstrap-grid.min.css">

    <link rel="stylesheet" href="css/style.css">
    <title>ShoppingList</title>
</head>

<body>
    <div class="container">
        <div class="box" id="heading">
            <h1>Shoppings</h1>
        </div>

        <div class="d-flex box">
            <div class="mainView">
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">Item</th>
                            <th scope="col">First Item</th>
                            <th scope="col">Last Second</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let i = 0; i < contactItems.length; i++) { %>
                        <tr>
                            <th scope="row"><%= i+1 %></th>
                            <td><%= contactItems[i].first_name %></td>
                            <td><%= contactItems[i].last_name %></td>
                            <td>
                                <form id="formDelete" action="/delete-contact" method="POST">
                                    <div class="delete-btn">
                                        <input type="hidden" value="<%= contactItems[i].id %>" name="contactDelete">
                                     
                                        <button type="submit" class="fas fa-times"></button>
                                    </div>
                                </form>
                            </td>
                        </tr>
                        <% } %>
                    </tbody>
                </table>

                <div class="table">
                   
                </div>

                <div class="d-flex justify-shopping-center ">
                    <form action="/add-contact" method="POST">
                        <input id="first_item1" type="text" name="newFirst" placeholder="Add first Item" value=""
                            required="true">
                        <br>
                        <input id="last_item2" type="text" name="newLast" placeholder="Add second item" value=""
                            required="true">
                        <br>
                        
                        <div class="d-flex justify-content-center ">
                            <button id="add-btn" type="submit" class="btn btn-primary submit-button">Add
                                Item</button>
                        </div>

                    </form>
                </div>

            </div>
        </div>

        <footer>
            <span class="footer">Rauno Järvela. &#169 2021</span>
        </footer>
    </div>
</body>
</html>